{
  "name": "medicalcheckup-subdomain",
  "version": "1.0.0",
  "description": "Public subdomain medicalcheckup.20fit.id — educational MCU pages + member analysis client that calls my.20fit.id. Zero runtime dependencies (Node built-in http).",
  "type": "module",
  "main": "src/server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "check": "node -e \"process.exit(0)\" && for f in $(git ls-files 'src/**/*.js'); do node --check \"$f\"; done"
  },
  "keywords": [
    "20fit",
    "medical-checkup",
    "mcu"
  ],
  "license": "MIT"
}
