// Single source of truth for the persisted theme-preference storage key,
// shared between the server-rendered pre-paint script (src/views/layout.js)
// and the client toggle handler (src/client/app.js).
export const THEME_STORAGE_KEY = "mcu20fit-theme";
